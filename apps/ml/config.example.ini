# Wave-Finder ML database configuration (example)
# Copy this to `config.ini` alongside the ML scripts.

[db]
host = 127.0.0.1
port = 3306
user = your_user
password = your_password
database = wavefinder
